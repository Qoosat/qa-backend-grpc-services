apiVersion: apps/v1
kind: Deployment
metadata:
  name: review-service
  namespace: dev
  labels:
    app: review-service
    component: grpc
spec:
  replicas: 2
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: review-service
  template:
    metadata:
      labels:
        app: review-service
        component: grpc
    spec:
      containers:
      - name: review-service
        image: nklrif/review-service:latest
        imagePullPolicy: Always
        ports:
        - name: grpc
          containerPort: 50051
          protocol: TCP
        - name: health
          containerPort: 8080
          protocol: TCP
        env:
        # Database configuration from secret
        - name: DATABASE_URL_POSTGRES
          valueFrom:
            secretKeyRef:
              name: qa-secret
              key: DATABASE_URL_POSTGRES
        # Database pool settings
        - name: DB_POOL_MIN_SIZE
          value: "2"
        - name: DB_POOL_MAX_SIZE
          value: "10"
        # Moderation Service endpoint
        - name: MODERATION_SERVICE_HOST
          value: "moderation-service"
        - name: MODERATION_SERVICE_PORT
          value: "50052"
        - name: MODERATION_TIMEOUT_SECONDS
          value: "5"
        # gRPC configuration
        - name: GRPC_SERVER_MAX_WORKERS
          value: "10"
        - name: GRPC_KEEPALIVE_TIME_MS
          value: "10000"
        - name: GRPC_KEEPALIVE_TIMEOUT_MS
          value: "5000"
        # Logging
        - name: LOG_LEVEL
          value: "INFO"
        resources:
          requests:
            cpu: 100m
            memory: 128Mi
          limits:
            cpu: 200m
            memory: 256Mi
        livenessProbe:
          exec:
            command: ["/bin/grpc_health_probe", "-addr=:50051"]
          initialDelaySeconds: 10
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          exec:
            command: ["/bin/grpc_health_probe", "-addr=:50051"]
          initialDelaySeconds: 5
          periodSeconds: 5
          timeoutSeconds: 5
          failureThreshold: 3
---
apiVersion: v1
kind: Service
metadata:
  name: review-service
  namespace: dev
  labels:
    app: review-service
spec:
  type: ClusterIP
  ports:
  - name: grpc
    port: 50051
    targetPort: 50051
    protocol: TCP
  - name: health
    port: 8080
    targetPort: 8080
    protocol: TCP
  selector:
    app: review-service
